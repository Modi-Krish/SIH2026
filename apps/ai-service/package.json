{
  "name": "@sapls/ai-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "venv\\Scripts\\uvicorn main:app --reload --port 8000",
    "build": "echo 'No build step for python'",
    "start": "venv\\Scripts\\uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
